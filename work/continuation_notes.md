# Continuation after 2026-09-20 solo pass

Read outputs/odyssey_extended_analysis.md first. All outputs in odyssey_extended_manifest.json verified against their hashes. Original SHA remains e2c3c5bb3746f417e70776967f8e1758bb3073fa551f553942a04e822c73ecf2. No firmware modifications.

Newest facts: 3,014,656 exhaustive cases passed in offline SLEIGH execution. CAN parser repeat check accepts any unequal old/new 2-bit counter, confirmed another4096 directed cases. Main curve maximum banks1-3=4864; banks4-7=4006 because1024 clamp makes high knots unreachable. Split fixed-point interpolation required; naive ideal model had130 one-count mismatches per bank4-7. Monitor flags latch after three corrupted snapshots and stay after counters recover.

Direct fault path: processed EF wordFFF89378 bit15 ->7541E->FFF830EC->72A44 state4/5->73852 weights0,16384,0 in35 synthetic scenarios. 74C9C CAN correction weighted byFFF82F64; baseline assist remains. GateFFF861ED bitsC0 can suppress flag publication. All256 gate-byte cases passed original segment4B658..4B8C6. Bit6 direct setter remains unlocated; normal directrefs write2,3,7. Beware low-ROM code near927E/986C calls RAMFFF861CA as executable code, so RAM addresses can represent different phases; these refs are not ordinary fault-data accesses.

Variant mode3 path76648 assigns state flags unconditionally; do not present77260 branch as confirmed fault-specific Odyssey mode3 behavior. Loader2F95A IDs THRA0/THRA1/THRX0/default disableFFF86E83; THRC0/THRA2/THRA3/THRA4/THRY0 enable it. Actual coding unknown. Caller17704 validates first19bytes of24byteFFF83F1C record; invalid markers copyROMdefault39798, setFFF85349, return1 (so return alone is not validity). 193single-bitcoding cases passed; no EEPROM writes done.

CRC nibble table5698=16entries ED B8 83 20 polynomial reflected, helper4E20 wrapper4E86, zlib-compatible. 4FE8 wrapper computes whole-range plus selected-range CRC, expected BE32 pointer atarg+16, whole outputarg+20, callbackarg+24,readcallbackarg+28. Count firstbytearg0;listpointerarg4,wholeStartarg8,lengtharg12. 12byte descriptors start+4,length+8. FullrangeC000..80000 offline test475136bytes7424reads resultBD19F157. ExpectedCRC/list were synthetic, not recovered real programming metadata. Descriptor52B4 references C000,74000,4FE8. Missing helper targets below2000 areFF; complete programming acceptance not established. Tail7FFF8=B7C8,7FFFC=FFFFA6E0 stillunknown. Do not claim regenerating known sum/CRC makes flashable image.

All validation code packaged outputs/offline_validation with original hash guard and rerun results separate. No exhaustive jobs remain running after completion; execsessions42774(CAN)and45265(fullCRC) completed. Source originals inwork. Avoid re-running long exhaustive tests without a new reason. The initial ideal-oracle discrepancy logs remain work/curve_execution_initial.log and work/monitor_execution_initial.log.

Next useful independent work: actual coding provenance/read path; complete programming context descriptor construction despite missing low code; physical-unit/controller coupling only with appropriate external measurements. Do not transplant Pilot V850 or Civic offsets. No validated2.5x modification exists.
